spring.application.name=reporting-service

spring.cloud.openfeign.client.config.default.connectTimeout=${CLIENT_CONNECT_TIMEOUT_MS:2000}
spring.cloud.openfeign.client.config.default.readTimeout=${CLIENT_READ_TIMEOUT_MS:3000}
spring.cloud.openfeign.client.config.default.loggerLevel=BASIC

spring.datasource.url=jdbc:mysql://${MYSQL_HOST:localhost}:${MYSQL_PORT:3306}/${MYSQL_DB:reporting_core}?sslMode=DISABLED&allowPublicKeyRetrieval=true&connectionTimeZone=UTC&forceConnectionTimeZoneToSession=true&characterEncoding=UTF-8
spring.datasource.username=${MYSQL_USER:reporting}
spring.datasource.password=${MYSQL_PASSWORD:reporting}
spring.datasource.hikari.maximum-pool-size=${DB_POOL_MAX:20}
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.connection-timeout=3000
spring.datasource.hikari.pool-name=reporting-pool

spring.jpa.open-in-view=false
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.properties.hibernate.jdbc.time_zone=UTC
spring.jpa.properties.hibernate.jdbc.batch_size=50
spring.jpa.properties.hibernate.type.preferred_uuid_jdbc_type=CHAR
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.order_updates=true

spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=false
spring.flyway.validate-on-migrate=true
spring.flyway.locations=classpath:db/migration

spring.data.redis.host=${REDIS_HOST:localhost}
spring.data.redis.port=${REDIS_PORT:6379}
spring.data.redis.timeout=2000ms
spring.data.redis.lettuce.pool.max-active=16
spring.data.redis.lettuce.pool.max-idle=8
spring.data.redis.lettuce.pool.min-idle=2

spring.kafka.bootstrap-servers=${KAFKA_BOOTSTRAP:localhost:9092}
spring.kafka.consumer.group-id=${KAFKA_GROUP:reporting-service}
spring.kafka.consumer.auto-offset-reset=earliest
spring.kafka.consumer.enable-auto-commit=false
spring.kafka.listener.ack-mode=manual
spring.kafka.producer.key-serializer=org.apache.kafka.common.serialization.StringSerializer
spring.kafka.producer.value-serializer=org.apache.kafka.common.serialization.StringSerializer
spring.kafka.producer.acks=all
spring.kafka.producer.properties.enable.idempotence=true

analytics.events.publish.enabled=${KAFKA_PUBLISH_ENABLED:false}
analytics.events.publish.report-generated-topic=reporting.report.generated
analytics.events.publish.settlement-calculated-topic=reporting.settlement.calculated
analytics.events.publish.settlement-updated-topic=reporting.settlement.updated
analytics.events.publish.schema-version=1.0

JWT_AUDIENCE=greatbus-api
spring.security.oauth2.resourceserver.jwt.issuer-uri=${JWT_ISSUER:GB-user-service}
spring.security.oauth2.resourceserver.jwt.jwk-set-uri=${JWT_JWK_SET_URI:http://localhost:8081/.well-known/jwks.json}
spring.security.oauth2.resourceserver.jwt.audiences=${JWT_AUDIENCE:greatbus-api}

server.port=${SERVER_PORT:8086}
server.error.include-message=never
server.error.include-stacktrace=never

management.endpoints.web.exposure.include=health,info,prometheus
management.endpoint.health.show-details=never
management.endpoint.health.probes.enabled=true
management.metrics.tags.application=reporting-service
management.metrics.distribution.percentiles-histogram.http.server.requests=true
management.metrics.distribution.percentiles.http.server.requests=0.5,0.95,0.99
management.metrics.distribution.slo.http.server.requests=50ms,100ms,300ms,800ms

analytics.dashboard.cache-ttl-seconds=${DASHBOARD_CACHE_TTL:60}
analytics.dashboard.stampede-lock-ttl-seconds=${DASHBOARD_LOCK_TTL:10}
analytics.dashboard.max-span-days=${DASHBOARD_MAX_SPAN_DAYS:92}
analytics.dashboard.contended-wait-millis=${DASHBOARD_CONTENDED_WAIT_MS:25}
analytics.dashboard.rate-limit-per-minute=${DASHBOARD_RATE_PER_MIN:120}

analytics.settlements.max-page-size=${SETTLEMENTS_MAX_PAGE_SIZE:200}
# Month-end settlement: 01:30 on the 1st, for the previous month.
analytics.settlements.schedule.enabled=${SETTLEMENT_SCHEDULE_ENABLED:true}
analytics.settlements.schedule.cron=${SETTLEMENT_SCHEDULE_CRON:0 30 1 1 * *}
analytics.settlements.schedule.zone=${SETTLEMENT_SCHEDULE_ZONE:Asia/Kolkata}

analytics.clients.enabled=${ANALYTICS_CLIENTS_ENABLED:false}
analytics.clients.fleet.url=${FLEET_SERVICE_URL:http://fleet-service.default.svc.cluster.local:8082}
analytics.clients.payment.url=${PAYMENT_SERVICE_URL:http://payment-service.default.svc.cluster.local:8084}
analytics.clients.service-token=${ANALYTICS_SERVICE_TOKEN:}

analytics.events.enabled=${ANALYTICS_EVENTS_ENABLED:false}
analytics.events.topics.schedule-updated=${TOPIC_SCHEDULE_UPDATED:schedule.updated}
analytics.events.topics.route-updated=${TOPIC_ROUTE_UPDATED:route.updated}
analytics.events.topics.booking-held=${TOPIC_BOOKING_HELD:booking.held}
analytics.events.topics.booking-confirmed=${TOPIC_BOOKING_CONFIRMED:booking.confirmed}
analytics.events.topics.booking-expired=${TOPIC_BOOKING_EXPIRED:booking.expired}
analytics.events.topics.booking-cancelled=${TOPIC_BOOKING_CANCELLED:booking.cancelled}
analytics.events.topics.payment-succeeded=${TOPIC_PAYMENT_SUCCEEDED:payment.succeeded}
analytics.events.topics.payment-failed=${TOPIC_PAYMENT_FAILED:payment.failed}
analytics.events.topics.refund-initiated=${TOPIC_REFUND_INITIATED:refund.initiated}
analytics.events.topics.refund-completed=${TOPIC_REFUND_COMPLETED:refund.completed}
analytics.events.replay.enabled=${ANALYTICS_REPLAY_ENABLED:true}
analytics.events.replay.poll-millis=${ANALYTICS_REPLAY_POLL_MS:30000}
analytics.events.replay.batch-size=${ANALYTICS_REPLAY_BATCH:50}
analytics.events.replay.max-attempts=${ANALYTICS_REPLAY_MAX_ATTEMPTS:10}

analytics.settlement.commission-rate=${ANALYTICS_COMMISSION_RATE:0.1000}
analytics.settlement.tax-rate=${ANALYTICS_TAX_RATE:0.0500}
analytics.settlement.calculation-version=${ANALYTICS_CALCULATION_VERSION:v1-default-rates}

# Generated report files belong in object storage so every instance can serve a
# download and nothing depends on the disk of one machine. Set REPORT_STORAGE
# to local-disk only for a throwaway local run with no bucket at all.
analytics.reports.storage=${REPORT_STORAGE:s3}
analytics.reports.local-disk-root=${REPORT_LOCAL_ROOT:./target/report-files}
analytics.reports.s3.bucket=${REPORT_S3_BUCKET:}
analytics.reports.s3.prefix=${REPORT_S3_PREFIX:reports}
analytics.reports.s3.region=${AWS_REGION:ap-south-1}
analytics.reports.s3.kms-key-id=${REPORT_S3_KMS_KEY_ID:}
analytics.reports.s3.endpoint=${REPORT_S3_ENDPOINT:}
analytics.reports.s3.path-style-access=${REPORT_S3_PATH_STYLE:false}
analytics.reports.lock-lease-seconds=${REPORT_LOCK_LEASE:900}
analytics.reports.max-span-days=${REPORT_MAX_SPAN_DAYS:366}
analytics.reports.max-active-per-user=${REPORT_MAX_ACTIVE_PER_USER:5}
analytics.reports.download-rate-per-user-per-minute=${REPORT_DOWNLOAD_RATE_USER:60}
analytics.reports.download-rate-per-report-per-minute=${REPORT_DOWNLOAD_RATE_REPORT:20}
analytics.reports.chunk-size=${REPORT_CHUNK_SIZE:1000}
analytics.reports.worker.enabled=${REPORT_WORKER_ENABLED:true}
analytics.reports.worker.poll-millis=${REPORT_WORKER_POLL_MS:2000}
analytics.reports.worker.batch-size=${REPORT_WORKER_BATCH:5}
analytics.reports.worker.recovery-millis=${REPORT_WORKER_RECOVERY_MS:60000}
analytics.reports.worker.processing-timeout-minutes=${REPORT_PROCESSING_TIMEOUT_MIN:15}

logging.structured.format.console=${LOG_FORMAT:ecs}
logging.structured.ecs.service.name=reporting-service
logging.level.root=INFO
logging.level.com.enterprise.analytics=INFO

eureka.client.service-url.defaultZone=${EUREKA_URL:http://localhost:8761/eureka/}
eureka.client.register-with-eureka=true
eureka.client.fetch-registry=true
eureka.instance.prefer-ip-address=true
eureka.instance.instance-id=${spring.application.name}:${server.port}
